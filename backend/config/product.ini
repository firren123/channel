project.name = product

[id]
type = id

[title]
type = title

[description]
type = body

[name]
index = both

[cate_first_id]
index = self
tokenizer = full

[cate_second_id]
index = self
tokenizer = full

[brand_id]
index = self
tokenizer = full

[total_num]
tokenizer = full

[warning_num]
tokenizer = full

[origin_price]
type = numeric
tokenizer = full

[sale_price]
type = numeric
tokenizer = full

[status]
index = self
tokenizer = full

[create_time]
tokenizer = full

[single]
index = self
tokenizer = full

[bar_code]
index = self
tokenizer = full

[attr_value]
index = both

[shop_price]
type = numeric
tokenizer = full

[sale_profit_margin]
type = numeric
tokenizer = full

[sales_num]
type = numeric
tokenizer = full

[image]
tokenizer = full